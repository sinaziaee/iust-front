import 'package:flutter/material.dart';

class CourseMaterialPage extends StatelessWidget {
  const CourseMaterialPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
