import 'package:flutter/material.dart';

class FinalProjectPage extends StatelessWidget {
  const FinalProjectPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
